I have defined goals as the thing I want to achieve in 80% of my games. E.g. 50 last hits at 10 minutes. However, this might not be clear to users, and they might want to have a target, e.g. Hit 90 last hits in 10 minutes and they might create it like that.

This task is to add a Goal type when creating a goal, asking the user if they want to hit this consistently or it's a goal they want to achieve once.

Alternatively, maybe the goal should have a percentage as well. e.g. "50 last hits at 10 minutes in 80% of my games", which should have a dropdown of `when` which has values like `Just once`, `in 80% of my games`, in `in 50% of my games` or `on average over a week`. 

need to expand this a bit